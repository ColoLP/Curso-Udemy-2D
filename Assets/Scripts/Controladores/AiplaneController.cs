using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AiplaneController : MonoBehaviour
{
    // Este script lo unico que va a tener es el movimiento del avion, y en el caso de que choque contra el Helicoptero lo matara,tira Game Over. 
    // Lo vamos a hacer para que tenga la sensasion de que se puede pasar por las nalgas todo. 
    // Update is called once per frame
    void Update()
    {
        
    }
}
